import {
  addItemToBoxWithId,
  createBox,
  createDumpForBoxWithId,
  fetchBoxWithId,
  fetchDumpWithId,
} from "./box";

export const api = {
  addItemToBoxWithId,
  createBox,
  createDumpForBoxWithId,
  fetchBoxWithId,
  fetchDumpWithId,
};
